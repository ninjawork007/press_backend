'use strict';

/**
 * publication-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::publication-category.publication-category');
