'use strict';

/**
 * publication-inquiry service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::publication-inquiry.publication-inquiry');
